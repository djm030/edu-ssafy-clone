# Context Snapshot: Next Step After ERD/SQL

- Task statement: Decide the next OMX/workflow step after ERD and SQL have been drafted for the eduSSAFY clone project.
- Desired outcome: Identify the immediate next artifact and workflow path before implementation begins.
- Known facts/evidence:
  - Workspace currently contains documentation and captured reference pages, not an application codebase.
  - ERD/SQL artifacts exist under `docs/`, including `ERD.md`, `ERD.sql`, `schema.sql`, `schema.dbml`, `revised_schema_mysql8.sql`, and `revised_schema.dbml`.
  - `docs/ERD.md` states that `docs/revised_schema_mysql8.sql` is the current preferred schema baseline and older schema artifacts are reference material.
  - `docs/FUNCTIONAL_SPEC.md` says the next step should proceed from functional spec toward screen list, API spec draft, and DB table detail design.
  - The project directory is not currently a git repository.
  - Some Korean markdown content appears mojibake in the shell, so document encoding should be normalized or verified before relying on text search.
- Constraints:
  - Do not start implementation until scope, API boundaries, and verification criteria are explicit.
  - Prefer a small MVP slice before broader team execution.
  - Windows OMX explore harness has a warning; use `omx sparkshell` or direct shell reads for local inspection.
- Unknowns/open questions:
  - Target tech stack is not present in the workspace yet.
  - Whether backend/frontend should be implemented now or docs should be finished first is not yet explicitly decided.
  - SQL has not been executed against MySQL 8 in this session.
- Likely touchpoints:
  - `docs/revised_schema_mysql8.sql`
  - `docs/revised_schema.dbml`
  - `docs/ERD.md`
  - `docs/FUNCTIONAL_SPEC.md`
  - `docs/REQUIREMENTS.md`
  - `docs/ROLE_MATRIX.md`
  - `docs/DOMAIN_SPLIT.md`
